<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_employee_management_employee_list" model="ir.ui.view">
        <field name="name">employee.management.employee.list</field>
        <field name="model">employee.management.employee</field>
        <field name="arch" type="xml">
            <list string="Employees">
                <field name="employee_code"/>
                <field name="name"/>
                <field name="job_title"/>
                <field name="department"/>
                <field name="email"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="view_employee_management_employee_form" model="ir.ui.view">
        <field name="name">employee.management.employee.form</field>
        <field name="model">employee.management.employee</field>
        <field name="arch" type="xml">
            <form string="Employee">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="Employee name"/></h1>
                    </div>
                    <group>
                        <group string="Work Information">
                            <field name="employee_code"/>
                            <field name="job_title"/>
                            <field name="department"/>
                            <field name="joining_date"/>
                        </group>
                        <group string="Contact Information">
                            <field name="email" widget="email"/>
                            <field name="phone" widget="phone"/>
                            <field name="active"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Notes">
                            <field name="notes" placeholder="Internal notes..."/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_employee_management_employee" model="ir.actions.act_window">
        <field name="name">Employees</field>
        <field name="res_model">employee.management.employee</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_employee_management_root" name="Employee Management" sequence="10"/>
    <menuitem id="menu_employee_management_employee" name="Employees"
              parent="menu_employee_management_root"
              action="action_employee_management_employee" sequence="10"/>
</odoo>
